language: R
r: release
cache: packages
sudo: required
pandoc_version: 1.19.2.1

git:
  depth: 5

branches:
  only: master

before_script:
  - chmod +x ./_build.sh
  - chmod +x ./_deploy.sh

script:
  - travis_wait ./_build.sh
  - travis_wait ./_deploy.sh

addons:
  apt:
    packages:
      - ghostscript
